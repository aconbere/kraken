if (typeof Kraken == "undefined") { Kraken = {}; }
if (!Kraken.objects) { Kraken.objects = {}; }

Kraken.objects.View = function (context) {
  //context.subscribe("start", function() { that.init(); });
};

